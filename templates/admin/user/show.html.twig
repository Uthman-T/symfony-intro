{% extends 'admin/base-admin.html.twig' %}

{% block title %}Show Page{% endblock %}

{% block body %}
    <h1>This is your user</h1>

    <div>
        <p>{{ user.id }}</p>
        <p>{{ user.username }}</p>
        <p>{{ user.email }}</p>
    </div>
{% endblock %}
